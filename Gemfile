# frozen_string_literal: true

source 'https://rubygems.org'

# Specify your gem's dependencies in servicelog.gemspec
gemspec

# Pin Rails to 7.0.x for development (minimum version compatible with Ruby 3.3)
gem 'rails', '~> 7.0.0'
